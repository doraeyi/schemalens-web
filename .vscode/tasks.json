{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "build dbschema (pnpm)",
			"type": "shell",
			"command": "pnpm --filter dbschema build",
			"group": "build",
			"problemMatcher": "$tsc"
		}
	]
}
